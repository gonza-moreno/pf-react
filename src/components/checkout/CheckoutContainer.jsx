import React from "react";
import Formulario from "./Formulario";

const CheckoutContainer = () => {
  return (
    <section className="flex justify-center items-center" id="checkout">
      <Formulario />
    </section>
  );
};

export default CheckoutContainer;
